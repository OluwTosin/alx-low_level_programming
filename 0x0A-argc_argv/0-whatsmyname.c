#include <stdio.h>

/**
 * main - that prints its name
 * @argc: argument count
 * @argv: argument v
 * Return: 0
 */

int main(int argc, char *argv[])
{
(void) argc;

printf("%s\n", argv[0]);
return (0);
}
